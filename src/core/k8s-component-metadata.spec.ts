import { K8sComponentMetadata } from './k8s-component-metadata';

describe('K8sComponentMetadata', () => {
  it('should create an instance', () => {
    expect(new K8sComponentMetadata()).toBeTruthy();
  });
});
