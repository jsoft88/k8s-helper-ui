import { K8sComponentContainerPort } from './k8s-component-container-port';

describe('K8sComponentContainerPort', () => {
  it('should create an instance', () => {
    expect(new K8sComponentContainerPort()).toBeTruthy();
  });
});
