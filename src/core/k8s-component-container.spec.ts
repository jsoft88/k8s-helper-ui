import { K8sComponentContainer } from './k8s-component-container';

describe('K8sComponentContainer', () => {
  it('should create an instance', () => {
    expect(new K8sComponentContainer()).toBeTruthy();
  });
});
